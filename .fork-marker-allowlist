# Baseline for dev/check_fork_markers.py — the fork-content audit.
#
# Format: `path/glob  # reason`. Every entry MUST carry a reason; the script
# refuses to run otherwise. This list is a ROLLOUT SCHEDULE, not a set of
# permanent exemptions: each entry means "the fork hunks in this file are not yet
# annotated", and the list is meant to shrink. Removing an entry is the definition
# of done for bringing a file under the convention.
#
# A file leaves this list one of two ways:
#
#   1. CONVERGE — make it byte-identical to upstream/main, retiring it as a
#      drop site forever. Only when our delta is genuinely not worth carrying.
#   2. MARK — put a `# Fork:` comment inside every diverging top-level
#      definition saying what deviates and why, or a fork boundary banner if the
#      fork content is a contiguous tail block.
#
# THE TRAP, and it is the whole reason this file has reasons rather than just
# paths: a site that differs from upstream is NOT automatically fork work. It can
# equally be content a merge resolution DROPPED, in which case marking it
# `# Fork:` launders a bug into a documented feature and no later audit will ever
# question it again. Establish provenance first (`git log -S`, `git merge-base
# --is-ancestor` against BOTH HEAD and upstream/main) exactly as AGENTS.md
# requires. Entries below tagged LEADS have known dropped-hunk candidates from
# `dev/find_dropped_hunks.py`; resolve those before marking anything in the file.
#
# Worked example of the trap, found while building this check:
# speculative/drafters/gemma4_assistant/masks.py was documented in AGENTS.md as a
# "deliberate divergence — must keep its absolute import". It was not. Upstream's
# a492e47d (#1494) changed masks.py to a relative import AND rewrote its test's
# module fakes to match; only the first half was dropped here, so our test still
# faked `mlx_lm.models.cache` (inert — the real mlx_vlm module loads anyway) and
# the absolute import was a workaround for the missing half. Taking BOTH halves
# made both files byte-identical to upstream with the test still passing. Marking
# masks.py `# Fork:` would have frozen that mistake permanently.
#
# RESOLVED 2026-08-09 (second pass), removed from this list:
#   models/qwen3_5_moe/qwen3_5_moe.py  — restored #1450's `mid` slicing (our copy had
#     #722's pre-fix `mx.split`); the unfused branch is fork work (f0d50c90, landed in
#     parallel with upstream's #1472) and now carries a `# Fork:` marker.
#   trainer/utils.py + tests/test_trainer_utils.py — both now BYTE-IDENTICAL to
#     upstream. 13d1ff4e unified the text-model LoRA path; our copy kept a separate
#     `adapter_utils.load_adapters` branch whose config handling was strictly weaker
#     (AttributeError on a missing `num_layers`, no `keys` targeting, legacy configs
#     skipped). Same-named test on each side, adapted to each implementation.
#   tests/test_utils.py — union-merged with upstream. Zero fork-only *definitions* but
#     real fork content in *bodies*, so not a clean checkout. Its 5 "pre-existing
#     failures" were stale test code; now 34 passed / 0 failed AND re-enabled in the
#     suite (the --ignore is gone).
#
#   tests/test_batch_quantized_cache.py — union-merged with upstream (fourth pass).
#     17 fork-only definitions, 12 upstream-only, and the one shared class differed
#     only by a method we lacked. Fork content is now two classes below a boundary
#     banner plus three marked imports; 12 .symbol-exclusions entries retired.
#
#   tests/test_speculative.py — union-merged (fourth pass). Only TWO fork-only
#     definitions in 3371 lines; the single shared test that differed did so by
#     4b15cc5's deliberate one-ULP bound, now marked. 11 .symbol-exclusions retired.
#
# RESOLVED 2026-08-10, removed from this list:
#   server/schemas.py — 1 site. `ChatStreamChunk` is upstream's field-for-field
#     (including cfcc36d9's GenerationTimings/StreamingTimings union); the only
#     deviation is the fork's `to_sse_json()` (a19e900f, fork-authored, absent from
#     upstream), which omits `timings` when unset because OpenWebUI's stream parser
#     dies on an explicit null. Marked.
#   generate/__init__.py — 4 sites. Re-export surface for the fork's generation-engine
#     port (5e9b9503) + maybe_preallocate_kv_cache (7de8f7f1), plus a lazy
#     `generation_stream` so importing the package does not create a Metal stream.
#     RESTORED first: `"GenerateKwargs"` was missing from __all__ — a30180a6's last
#     dropped line — and the import was therefore unreferenced, i.e. autoflake bait
#     (trap 5). Marking that site would have laundered it.
#   models/gemma4/language.py — 5 sites -> 3 markers, and TWO were dropped upstream
#     content, not fork work: 64a09079 (#1445)'s `base_mask.shape[-2] != [-1]` guard on
#     the cached vision-mask overlay, and upstream's comment above
#     `use_bidirectional_vision`. Both restored, so the file now diverges only by
#     EpiCache + suffix decoding. Worth knowing: that comment describes
#     `has_audio_tokens` from 2197a68a, an upstream BRANCH commit that never reached
#     upstream/main — so it is stray in both trees. Restored verbatim anyway; matching
#     upstream byte-for-byte is what keeps future merges clean.
#   tests/test_server.py — 6 sites. Two were PLACEMENT only (upstream tests with
#     byte-identical bodies sitting at fork-shifted line numbers); the rest are the
#     fork's /v1/completions endpoint tests, its position-keyed sampler test, and the
#     THINKING_FORMATS additions to TestThinkingStreamState.
#   server/cli.py — 6 sites. ~17 fork flag groups in one parser, so one marker on
#     `main()` covers them; the rest are fork imports. NB --log-progress-interval is
#     upstream's (cfcc36d9), not fork — the marker says so.
#   server/app.py — 7 sites. All fork, and two are strict supersets worth naming:
#     `_count_thinking_tag_tokens` preserves upstream's hard-coded accounting exactly
#     and adds a registry fallback, and `unload_model_sync` additionally STOPS the
#     generator thread and clears APC / vision / session caches, each of which pins
#     the unloaded weights' allocations. `_extract_response_format_schema` is
#     byte-identical — placement only.
#   server/responses_state.py — 5 sites. Zero upstream lines missing (pure additions),
#     which is what made marking safe: the THINKING_FORMATS registry integration,
#     `_CONTENT_MARKERS` stripping, and the ends-with-a-prefix partial-tag scan.
#   speculative/utils.py — 5 sites, all one feature: `draft_kind == "suffix"`
#     dispatch to the fork's drafter-free n-gram speculation (863441c9), which
#     upstream does not have at all. 61+/0- means pure additions, so there was no
#     upstream code to have dropped. Marked; note the `__all__` marker has to sit
#     INSIDE the list literal — a module-scope hunk is keyed by line, so a marker
#     above the statement does not cover it.
#   models/qwen3_5/language.py — 7 sites -> 3 marked regions, and THREE of its four
#     diverging areas were dropped upstream content rather than fork work, which is the
#     highest ratio any file in this rollout has produced:
#       * 16cf6140 (#1423, "Qwen3.5 on CUDA: fix Metal-only crashes") — all THREE of its
#         language.py hunks, while its gated_delta.py half is byte-identical. So the
#         `mx.metal.is_available()` guards on `_TARGET_VERIFY_GEMV` and on
#         `_qwen3_5_ragged_decode_attention` were both gone. Unreproducible on Apple
#         Silicon by construction, which is why it survived; guarded now by monkeypatching
#         the predicate the hunks consult.
#       * 7fbc7bc9 (#1598) split `_can_target_verify_quantized` into a weight-only
#         `_can_target_verify_quantized_head` plus a thin caller. The extraction landed and
#         the REWRITE of the original was dropped, so the pre-split body survived as a
#         second copy of the same predicate. Provably equivalent, so no behavioural repro.
#       * ea5ad0e4 (#1455) touched 11 model files with the same 4-line empty-MRoPE guard;
#         qwen3_5 was the ONLY one missing it. The fork then re-fixed the same crash by
#         hand at a different place in the function (d7eb079c), equivalently — so the
#         suite was green and upstream's own parametrized test_rope.py passed either way.
#         Converged to upstream's position; the fork's duplicate is gone.
#     What actually remains is EpiCache (150a76f9: `_qwen35_scalar_positions` +
#     `cache.observe`, `make_cache`), suffix decoding (ff0ecc30/de4e502b:
#     `suffix_verify_kwargs`, `chunked_prefill_policy`'s `"suffix"` arm and
#     `no_chunked_prefill`) and turboquant KV (a62f7cb8: the verify loop's `bits` guard).
#     Missing upstream lines went 30 -> 6, and all 6 are the EpiCache/turboquant rewrites.
#     NB AGENTS.md's note that this file's norm-shift gate was protected fork work was
#     already known wrong; nothing here depended on it.
#   turboquant.py — 7 sites -> 4 markers + 2 module-level, and the AST-body method
#     found the one real defect that three earlier passes over this file missed.
#     91 of 96 shared definitions were byte-identical, so the whole review reduced to
#     five, and this file's own entry had already been corrected once for reading the
#     per-commit report instead of a direct diff. RESTORED: e3906673 (#1433, "improved
#     turbo quant's prefill") is a single-file 8-line commit whose two Metal-kernel
#     changes landed and whose third did not — it WIDENED
#     `_TurboQuantMSECodec.quantize`'s fast path from
#     `vectors.shape[-2] == 1 and self.bits > 0 and self.use_rht` to `self.bits > 0`,
#     and this tree kept the pre-#1433 narrow gate. So every multi-token call, i.e.
#     every prefill, fell past BOTH fused-kernel blocks to the slow `_quantize_unit`
#     path: exactly the prefill the commit title improved. Not a missing hunk — a
#     narrowing upstream had already removed, which is why nothing read as absent.
#     All 241 turboquant tests pass either way, because both paths produce a valid
#     quantization and none asserts which ran. `quantize` is now byte-identical to
#     upstream; guard: TestTurboQuantPrefillUsesTheFusedKernel.
#     The other four are genuine fork work, all `git log -S`-confirmed:
#       * `_fused_mse_decode_2pass_1_kernel` is a fork rewrite (bf7c793a, GQA
#         tile-reuse). Upstream's kernel is NOT lost — it is
#         `_fused_mse_decode_2pass_1_kernel_legacy`, byte-identical apart from the
#         rename and an added docstring, and TurboQuantKVCache branches between the
#         two at runtime. Keeping it verbatim means a future upstream change to that
#         kernel conflicts in that function instead of diverging silently.
#       * `_reserve_state_capacity` is a strict superset (2f5e01dd): upstream's step
#         arithmetic unchanged, max()'d with a geometric 1.25x plus allocation hooks.
#       * `TurboQuantKVCache` adds four fork-only tiled-prefill methods (b311af00).
#       * `BatchTurboQuantKVCache`'s 73 "missing" lines are upstream's SHADOWED DEAD
#         `zero_row_tail` — upstream defines it twice and the second shadows the
#         first; ours is byte-identical to the live copy. This is handoff §4's
#         1171888e note, now confirmed mechanically rather than by spot check.
#     Also converged: the absolute `from mlx_vlm.models.cache import ...` (9065b024),
#     the same artifact apc.py had and the third instance of that shape in this file.
#   utils.py — 12 sites -> 0, THE LAST ENTRY, and it closed on a decision rather than a
#     discovery. Its three dropped hunks were restored in c5454e71 (see below); what kept
#     it listed for a session was b93707d8 (#1498)'s OTHER half — prepare_inputs'
#     qwen3-omni audio special case, CONFIRMED-STALE (upstream's deleted text verbatim,
#     no fork commit ever touched it, its model-side half landed byte-identical) but with
#     zero test coverage, since nothing in tests/ passed `audio=` to prepare_inputs.
#     DELETED 2026-08-10 on an explicit decision. The coverage that was missing is now
#     written: TestOmniAudioFeaturesComeFromTheProcessor asserts the contract the
#     simplified path depends on — the processor receives audio ARRAYS and whatever it
#     returns is forwarded — plus a tripwire against the branch reappearing, which
#     matters because a future merge re-offering pre-#1498 content produces NO conflict
#     (its deleting commit is already an ancestor of main).
#     Missing upstream lines 32 -> 12, and all 12 are the fork's print/logging ->
#     module-logger conversions (2f5e01dd, confirmed fork-authored), so every marker in
#     this file names that plus whatever else its function carries.
#     Worth keeping: holding this open for a session with the evidence written down was
#     the right move, not a stall. Marking it would have laundered stale code into a
#     documented feature; deleting it blind would have been an unvalidated behaviour
#     change. Writing down what was known and what could not be known LOCALLY is what
#     turned it into a one-line decision later.
#   server/openai.py — 16 sites -> 0. RESTORED one dropped hunk that is the cleanest
#     "helper landed, ONE OF TWO call sites dropped" instance found yet: eda1ec4f
#     (#1716, "Fix Responses instructions for Qwen templates") replaced the same
#     `chat_messages.insert(0, {"role": "system", ...})` in BOTH /v1/responses and
#     /v1/responses/input_tokens with a call to the new
#     _normalize_response_instruction_messages, which coalesces `instructions` plus
#     every system/developer message into ONE leading system message and drops the
#     originals. The helper landed, responses_endpoint's call site landed,
#     responses_input_tokens_endpoint's did not. So the endpoint whose entire contract
#     is to report the token count of the prompt /v1/responses builds was counting a
#     differently-shaped list — instructions duplicated as a second system message and
#     `developer` roles left in place, which is exactly what #1716's title says breaks
#     Qwen templates.
#     TWO reasons nothing caught it, both worth carrying forward: check_dead_helpers.py
#     is per-symbol so the surviving caller made the helper read as reachable; and
#     #1716's own two tests DID land and DO pass, because both exercise /v1/responses.
#     **A restored upstream test that covers the call site which survived is worse than
#     no test — it makes the commit look closed.** When a commit changes N call sites,
#     count them (`git grep -c`) instead of trusting its tests.
#     Also CONVERGED a relocation: _normalize_response_instruction_messages was
#     byte-identical to upstream but sat ~900 lines further down, which produced a
#     deletion hunk AND an addition hunk (2 of the 16 sites). Moving it to upstream's
#     position closed both — the same reorder-not-rewrite finding as apc.py, in a file
#     that is otherwise a genuine rewrite.
#     27 of 31 shared definitions were byte-identical. The 4 that differ:
#     chat_completions_endpoint (753 -> 832; its 38 absent lines are exactly two
#     substitutions — `model_dump_json` -> the fork's `to_sse_json`, 7 of them, and
#     ThinkingStreamState.feed -> _step_thinking_state), responses_endpoint (6 lines,
#     same thinking substitution), responses_input_tokens_endpoint (now fixed) and
#     register_routes (the /v1/completions route). 13 ours-only definitions, almost all
#     the fork-only /v1/completions endpoint and the THINKING_FORMATS prefill helpers.
#     Confirmed fully landed and NOT to be re-derived from this file's divergence:
#     4d468e8575 (#1492) video plumbing and b3d2380d1d (#1266) per-token thinking split,
#     both of which the allowlist previously listed as open leads here.
#   server/generation.py — 13 sites -> 0, and BOTH of its "unreviewed"
#     .symbol-exclusions entries are now reviewed, which is what actually unblocked it:
#       * `_check_configured_context_budget` — deliberately removed by fork commit
#         02e8046d, which replaced upstream's reject-if-too-long with clamp-to-remaining
#         -context (_resolve_generation_budget / _apply_generation_budget /
#         get_min_output_tokens, plus THINKING_BUDGET_CLAMP_RATIO), covered by the
#         fork-only tests/test_context_budget.py. Do NOT restore it: running both would
#         reject requests the clamp exists to serve.
#       * `_sample_top_p_one` — superseded, not dropped. Upstream's
#         _PositionedTargetSampler applies top_p ONLY via that hand-rolled per-row
#         method; the fork replaced it with a _filter() applying top_p / min_p / top_k
#         in sample_utils.make_sampler's chain order, so _make_sampler stops silently
#         dropping min_p and top_k on the ragged-verification path. Strict superset.
#     RESTORED first, and this is the third pass at the same commit: cfcc36d9 (#1634)
#     was declared whole in 682043f5 (14 lines short), finished in 59d0229a against the
#     wide pass, and STILL had two hunks missing — logger.info("Generation cancelled:
#     ...") in the batch loop's cancellation path and one of three "Prefill completed:"
#     sites (the speculative one). Both are DIRECT logger.info calls in blocks otherwise
#     byte-identical to upstream, which is why the existing guard counting `self._log_*(`
#     call sites passed throughout. A log line has no caller, no symbol and no test, so
#     counting the commit's own MESSAGE LITERALS across both trees is the only handle —
#     now a guard (test_every_log_message_literal_appears_as_often_as_upstream).
#     43 of 47 shared definitions were byte-identical; the four that differ are
#     QueuedGenerationRequest (+2 fields), _PositionedTargetSampler (the _filter
#     superset), _TokenIterator (a `while True` for KeepAlive heartbeats) and
#     ResponseGenerator (a 1348 -> 1740 rewrite). NB _PositionedTargetSampler's
#     docstring already said "Fork:" and did NOT satisfy the check — marker mechanic 2.
#   tests/test_generate.py — CONVERGED by REORDERING, 5 sites -> 0, and it settles the
#     open question in this file's own previous entry, which said "either reorder the
#     fork's methods to upstream's positions, or leave this entry" and warned that a
#     marker on a whitespace probe line creates a NEW probe line (verified then:
#     10 -> 8 -> 1 -> 2 sites while chasing). Reordering is the answer, and the AST
#     comparison is what makes it safe rather than fiddly.
#
#     47 of 49 shared definitions were byte-identical and BOTH differing ones were
#     accounted for: `TestBatchGenerator` had zero body differences and one fork-only
#     method, and `TestPrefixCacheReuseTrim` shares ZERO method names with upstream
#     because the fork replaced the class wholesale (c503fa7b) — all 9 of upstream's
#     methods are REVIEWED `.symbol-exclusions` entries testing
#     `_prefix_cache_trim_amount` / `_cache_fully_retained`, which this fork does not
#     have. So the whole delta was position:
#       * `TestBatchGenerator`'s methods reordered to upstream's order, the one
#         fork-only method appended last;
#       * four top-level tests that upstream ALSO has were sitting after the
#         `if __name__ == "__main__":` guard; moved to their upstream positions and the
#         guard moved to the end of the file, where upstream has it;
#       * one banner COMMENT had to swap with the test below it. Comments are not AST
#         nodes, so a node-based reorder puts a relocated def on the wrong side of one
#         — check the banner positions by eye afterwards.
#     Missing upstream lines 251 -> 70, and all 70 are TestPrefixCacheReuseTrim's
#     excluded methods. Verified by AST-diff before against after (same def set, zero
#     bodies changed apart from TestBatchGenerator's member order) AND by
#     `pytest --collect-only`: 112 tests before, 112 after. Do the collect-only check
#     on any test-file reorder — it is the only thing that catches a shadowed
#     definition, which is how TestLagunaProcessor hid a missing symbol.
#   apc.py — CONVERGED, not marked, and the reason is the most reusable thing in this
#     file. It was the largest entry here for weeks (36 sites / 75 hunks, 554 missing
#     upstream lines against 567 added) and every description of it — this file's own
#     entry, the handoff's §2g — called it "a rewrite rather than a set of hunks" and
#     budgeted it accordingly. It was not a rewrite. It was a REORDERING.
#
#     What settled it in one step: AST-compare the two files def by def. Of 76 shared
#     top-level definitions, **74 had byte-identical bodies**, all 23 module-level
#     statements were identical, and the whole 554-line delta was four block moves left
#     over from the fork's own cluster-by-cluster port (0f359e53). The genuine fork
#     content was FOUR things: `_kv_prealloc_floor` + `_resolve_turn_capacity`
#     (fork-only, 739f7ff1 / 3c8bf750) and two `min_capacity_tokens=max(...,
#     _kv_prealloc_floor())` call sites in APCManager. Two further "divergences" were
#     artifacts: `resident_bytes`/`_resident_bytes_locked` sat at a different position
#     inside APCManager, and `_clone_layer_major_kv_cache_for_apc` had an ABSOLUTE
#     `from mlx_vlm.models.cache import KVCache` where upstream is relative — the only
#     absolute mlx_vlm import in the file, left by 9065b024, i.e. the same
#     conversion-artifact shape as the masks.py non-divergence above.
#
#     So the exit was §2f's test-file method applied to a library file: take upstream's
#     file as the base, re-apply the fork content, mark it. `git diff upstream/main --
#     mlx_vlm/apc.py` is now THREE hunks, all marked. RESTORED on the way in, both from
#     3de5bada (#1566), whose other three files had all landed: the
#     `apc_trace("store", ...)` call site (see below) and `_env_truthy("APC_DISK_TRACE")`.
#
#     THE LESSON: before sizing a big diverged file, AST-diff the definition bodies.
#     "554 missing lines" and "36 sites" measure alignment, not content, and for a
#     reordered file they measure it wrongly by two orders of magnitude. Do this for
#     turboquant.py (118/529) and dispatch.py (125/377) before budgeting either.
#
#     The store trace also deserves its own note, because it beat all six audits at
#     once and is the cleanest "helper landed, call site dropped" in the tree: parity
#     and symbols saw `apc_trace` present, deletions saw nothing removed, fork-markers
#     had apc.py allowlisted, and `check_dead_helpers.py` is PER-SYMBOL — `apc_trace`
#     has two other callers, so it read as reachable. And #1566's own restored test
#     calls `apc_trace("store", ...)` DIRECTLY rather than driving `store_exact_cache`,
#     so it passed throughout. With APC_TRACE=1 the store event simply never logged,
#     against a README that promises it. Guard:
#     test_dropped_upstream_guards.py::TestApcStoreEmitsItsTrace.
#
# ============================================================================
# EMPTY as of 2026-08-10. The rollout is COMPLETE: 0 files, down from 32 files /
# 175 sites. Every diverged file this fork carries is now either byte-identical to
# upstream or carries a `# Fork:` marker saying what deviates and why.
#
# KEEP THIS FILE AND ITS HEADER. It is a rollout schedule, and the schedule being
# empty is the steady state to maintain, not a reason to delete the mechanism — the
# next merge that adds a fork hunk to a shared file will need an entry, exactly as
# server/request_normalization.py did. The RESOLVED notes below are the record of how
# each file was settled and are the reason a future reader can trust the markers
# rather than re-deriving them.
#
# Two coverage rules are now doing work that no marker can, and both are visible in
# every `--summary` run rather than silent:
#   * check_fork_markers.py case 4 — a deletion-only hunk whose removed def/class
#     names are ALL excused in .symbol-exclusions. Three symbols across two files.
#   * the fork-boundary banner in models/cache.py and the test-file banners.
# ============================================================================
# Every
# diverged file NOT listed here is marked, including
# server/request_normalization.py — the gen-args union added fork hunks to a file
# that had been byte-identical to upstream, and this check caught them before the
# commit landed, which is the workflow working as intended.
#
# SITE COUNTS BELOW ARE FROM `--summary` AND ARE NOW ACCURATE. They were not:
# every count in this file was stale (apc.py said 100 sites against a real 36,
# schemas.py said 10 against a real 1). `--summary` could not check them because it
# deleted allowlisted paths before building its table — fixed 2026-08-10, so it now
# prints a second, ranked table of exactly these files. Re-run it rather than
# trusting a number here; that is the same discipline the handoff's §1 uses.

# --- Heavy fork rewrites. AGENTS.md names these as the false-positive-prone
# --- files; site-by-site provenance is the bulk of the remaining work.

# --- Server surfaces, not yet reviewed at site granularity.

# --- Library modules, not yet reviewed at site granularity.
# --- TRIAGE (2026-08-10). Missing upstream lines / added lines per file, from a direct
# --- `git diff upstream/main`. Use this to ORDER the work; it decides nothing on its own
# --- (AGENTS.md's first rule), but a file with few missing lines is cheap to clear and
# --- a file with many needs each one accounted for before any marker goes in:
# ---   [common.py 28/848 CLEARED] · [qwen3_5/language.py 30/121 CLEARED, now 6/94]
# ---   [utils.py 32/171 REVIEWED, now 19/159, one named blocker]
# ---   [apc.py 554/567 CONVERGED, now 2/35 -- it was a REORDERING, not a rewrite;
# ---    AST-diff bodies before sizing any file here, see its RESOLVED note above]
# ---   [openai.py 76/905 CLEARED: 27 of 31 bodies byte-identical; one dropped call
# ---    site (#1716) and one byte-identical function that had merely MOVED]
# ---   [generation.py 92/665 CLEARED: 43 of 47 bodies byte-identical; both its
# ---    "unreviewed" .symbol-exclusions entries reviewed; cfcc36d9 took a THIRD pass]
# ---   [turboquant.py 118/529 CLEARED: 91 of 96 bodies byte-identical; the AST-body
# ---    method found #1433 where three earlier report-based passes had not]
# ---   [dispatch.py 125/377 CLEARED: fully marked, and its last deletion-only hunk is
# ---    now covered by check_fork_markers.py case 4]
# ---   [test_generate.py 251/297 CONVERGED by reordering, now 70/116; all 70 are
# ---    TestPrefixCacheReuseTrim's 9 REVIEWED .symbol-exclusions methods]
# --- ar.py was 26/233 and is now cleared: all 26 were rewrite artifacts, confirmed by
# --- equal-count spot checks (speculative_prefill_capture_kwargs 6/6, BatchKVCache 7/7,
# --- logging.getLogger 1/1) plus fork-only _get_generation_stream (4/0).
# --- qwen3_5/language.py was 30/121 and is now cleared, but NOT the way ar.py was: the
# --- identifier-count technique flagged `is_available` 0/2, `backends` 0/1 and `Metal` 1/2,
# --- and every one of those unequal counts was a real dropped hunk. Equal counts mean
# --- "rewrite artifact"; UNEQUAL counts are the whole signal, and this file is the case
# --- that proves it pays to chase them rather than eyeball the diff. Now 6/94.

# --- Tests, not yet reviewed at site granularity.

# --- [The TOOL LIMITATION recorded here on 2026-08-09 is FIXED.] It said two files
# --- were fully marked yet un-drainable because a pure-DELETION hunk cannot carry a
# --- marker, and that teaching the check to consult `.symbol-exclusions` was the
# --- cheapest remaining tooling win. Done: `check_fork_markers.py` now has a case 4 —
# --- a deletion-only hunk whose removed top-level symbols are ALL already excused in
# --- `.symbol-exclusions` counts as covered, and the script names them on every run so
# --- the coverage is auditable rather than silent. `generate/dispatch.py` drained on
# --- the spot. The rule is narrow by construction (>= 1 top-level def AND all names
# --- excused), so a whitespace-only deletion still reports and a deletion touching any
# --- unexcused symbol still reports; `mlx_vlm/tests/test_fork_marker_check.py` is the
# --- other file this unblocks — its `_check_configured_context_budget` is an existing
# --- exclusion entry.
