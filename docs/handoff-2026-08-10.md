# Handoff: the dropped-upstream backlog (current as of 2026-08-09, end of fourth pass)

**Temporary working document.** `AGENTS.md` is the rules; `docs/upstream-gaps.md` is
the durable record and the taxonomy of failure shapes; `memory.md` has the per-commit
change log. This file is only *what is left, in what order, and what will bite you*.
**Delete it when the list in §2 is empty** — it is not.

Read before touching anything: `AGENTS.md` in full (it is short), then
`upstream-gaps.md`'s "Tooling that now guards this", "The union cluster",
"`8422ece8` — the APC adapter refactor", "Per-file convergence" and "The deliberate
divergence that wasn't".

---

## 1. Status

* **61 commits on `main`, not pushed.** Leave them that way unless told otherwise.
  Pushing needs explicit per-push permission.
* Upstream base **`ffd7aeff`**. Working tree clean.
* Suite: **2623 passed / 5 skipped / 0 failed.** The only `--ignore` left is
  `tests/test_smoke.py`.
* **Six audits** — five gating, all green. Baselines:

  | baseline | now | was |
  |---|---|---|
  | `.symbol-exclusions` | **57** | 107 |
  | `.deletion-exclusions` | **4** | 11 |
  | `.fork-marker-allowlist` | **19 files** | 32 |
  | `.dead-helper-exclusions` | **0** | (new this pass) |

* `find_dropped_hunks.py`: **12** commits with missing content, down from 34 — and
  **four of the twelve are closed by review, not open work** (§4). Real remaining
  content is `cfcc36d9` 375 lines, `29b6c00b` 114, `221fe0b3` 61, and ~90 lines
  across two test files.

**First action every session, before anything else:**

```bash
git fetch upstream && git log --oneline HEAD..upstream/main
```

Non-empty → merge first, even mid-task. It was non-empty at the start of the last
session for the first time, so the weekly rule is load-bearing now, not theoretical.

---

## 2. What is left

### 2a. `cfcc36d9` (#1634) server generation logging — 375 lines, 7 files

The largest dropped commit left, and the only large *library* one.
**Approved 2026-08-09: take upstream whole, default interval included.** Verified
before approving that it logs metadata only — `prompt_tokens`, `generated_tokens`,
`cached_tokens`, elapsed, tok/s, `finish_reason`, `in_flight`, backend, model and
media *counts*. No prompt text, no generated text, no per-token detail; `request_id`
is a memory-address hex, not a content hash.

Surfaced by the 2026-08-09 merge: `server/__init__.py` conflicted over importing
`get_log_progress_interval`, which exists **nowhere** in this tree. The merge
resolved it by omitting the name rather than restoring one file of a seven-file
commit.

Owns 7 `.symbol-exclusions` entries still marked "unreviewed"
(`get_log_progress_interval`, `_log_prefill_started`, `_log_prefill_progress`,
`_log_prefill_completed`, `_log_decode_progress`, `_request_log_id`,
`tests/test_server.py::test_log_progress_interval_is_configurable`) plus
`server/schemas.py::StreamingTimings`.

Files: `README.md`,
`server/{__init__,app,cli,generation,openai,responses_state,schemas}.py`,
`tests/test_server.py`. `generation.py` alone is ~221 missing lines and is the file
`AGENTS.md` names as the false-positive-prone fork rewrite, so expect to interleave
rather than `git checkout`. **Restore whole or not at all.**

### 2b. `f044f36a` (#1359)'s **third file** — 18 lines, do this one first

**A regression I introduced by half-restoring a commit, found 2026-08-09 after the
fact.** `f044f36a` was restored last pass — but only its `generate/dispatch.py` and
`server/openai.py` halves. Its **`server/generation.py` half was missed**: an
18-line hunk adding `skip_special_tokens: bool = True` and the
`skip_special_token_ids` gate, and replacing two hardcoded `skip_special_tokens=True`
with `args.skip_special_tokens`.

**Why it was missed, and this is the reusable part:** `find_dropped_hunks.py`
reported only dispatch.py (9 lines) and openai.py (3). The `generation.py` hunk is
mostly 1–2 line changes, which fall **below that script's `--min-lines 3` Python
floor**. A real behavioural regression sat under the detector's own threshold — the
same blind spot that `--min-lines-config 1` was added for on the docs/config side.
Run a second pass at `--min-lines 1` before calling the report clean (§6).

This is also what `upstream-gaps.md` **item 9's diffusion half actually is** — a
two-line regression fixable directly, *not* something blocked behind 2c. Fix it
either standalone or for free by taking 2c.

> **Still do not implement `skip_special_tokens` on the normal text path.** That half
> of item 9 remains an upstream bug: upstream honours the flag only inside
> `_generate_diffusion`, and upstream's `anthropic.py:489` is byte-identical to ours,
> so the write is inert **in both trees**. Report it upstream; do not invent the
> behaviour here.

### 2c. `29b6c00b` (#1508) diffusion unification — 114 lines

**No decision needed — it is stale upstream code, not fork work.** Retires the three
remaining `.deletion-exclusions` entries
(`server/generation.py::_generate_masked_diffusion` plus its nested
`flush`/`on_block`). Missing: `tests/test_server.py` 87, `server/schemas.py` 27.

An earlier version of this handoff called it "a rewrite of the fork's diffusion
serving path, higher blast radius." **That was wrong**, and the provenance is worth
keeping because the wrong framing nearly deferred a straightforward restore:

* Every commit that ever touched `_generate_masked_diffusion` is authored by the
  upstream maintainer. The three that are not ancestors of `upstream/main` are
  unsquashed upstream branch commits — **"not an ancestor of `upstream/main`" is not
  the same as "fork-authored"**, and that is a trap in its own right.
* The conclusive test: our copy differs from upstream's last pre-deletion copy in
  **exactly two spots**, both the `args.skip_special_tokens` → `True` regression in
  2b above.

So the fork has no diffusion path to protect. Taking upstream is a plain
STALE → current replacement, the same category as the `_merge_arrays_cache_entries`
deletion, not a redesign.

### 2d. The last two test-file unions

`tests/test_server.py` (~160 missing lines, 31 exclusions, 124 fork-only
definitions) and `tests/test_generate.py` (~45 lines, 22 exclusions).

`test_server.py` is **partly gated on 2a and 2c** — much of its missing content is
their test halves. `test_generate.py` is not gated: the library halves of
`a30180a6`, `ea2edd68` and `e029e2b3` all landed last pass, so only upstream's tests
for them are absent, and `c503fa7b`'s 51 lines there are *superseded*, not missing.

**The method is four for four** (`test_utils.py`, `test_trainer_utils.py`,
`test_batch_quantized_cache.py`, `test_speculative.py`). The order of the steps is
what makes it safe:

1. AST-compare definition names both ways. **`only_ours` empty does NOT mean
   checkout-safe** — `test_utils.py` had zero fork-only definitions and still had
   real fork content in *bodies*.
2. Diff the *bodies* of shared definitions. Anything ours-only is either a fork
   adaptation (keep it, mark `# Fork:`) or a weakened assertion (take upstream's).
   These look identical in a diff; `memory.md`'s change log is what distinguishes
   them — `test_speculative.py`'s one-ULP bound is a legitimate relaxation recorded
   in `4b15cc5`.
3. Take upstream's file as the base so future merges apply cleanly, then re-append
   fork-only definitions below a `# Fork additions below this line` banner.
4. Union the import block, marking fork-only imports (`# Fork:` inline — a
   standalone comment gets hoisted by isort).
5. Re-run `check_upstream_symbols.py` and `check_fork_markers.py` and prune whatever
   stops being excused, **in the same commit**.

### 2e. Then drain `.fork-marker-allowlist` (19 files)

`python dev/check_fork_markers.py --summary` ranks them. Two exits per file:
converge to byte-identical, or mark every diverging top-level statement.

`apc.py` (100 marker sites, the largest) is **newly approachable** — its owning
commit `8422ece8` was resolved last pass, so its `LEADS` tag is stale.

> **Read the trap in that file's header before marking anything.** A site that
> differs from upstream is *not* automatically fork work; it can be dropped content,
> and marking that `# Fork:` launders a bug into a documented feature no later audit
> will question. Marking requires the same `git log -S` provenance as restoring.

---

## 3. The pattern that produced most of last pass's real bugs

Worth internalising before you start, because it is still the most likely thing you
will find: **"the symbol exists and nothing calls it."**

Missing *wiring*, not missing code. The helper is present, importable, and usually
unit-tested — so the suite is green and the audits are quiet. Five instances last
pass, three user-visible: embedding models silently ignored their
`1_Pooling/config.json`; the APC cache key omitted audio/video/embeddings/masks so
requests differing only in media **shared a prefix cache**; three sampler modes were
accepted by the API and discarded.

`dev/check_dead_helpers.py` now gates that shape (~10s, currently 0 exclusions).
When something looks unused, ask whether **upstream** calls it before concluding it
is fork scaffolding.

---

## 4. Closed — do not reopen

These keep appearing in `find_dropped_hunks.py`. That is correct; the report is a
lead generator and these leads have been followed.

* **`c503fa7b`** (88 lines, ranked 3rd) — reviewed deliberate divergence. The fork
  guards rotating-cache prefix reuse with `_rotating_rewind_safe` + a snapshot ring.
  Running upstream's `_prefix_cache_trim_amount`/`_cache_fully_retained` alongside it
  would give two different notions of when a rewind is safe. Its 51
  `tests/test_generate.py` lines are the tests *for those symbols*, superseded by
  `TestPrefixCacheReuseTrim`.
* **`1171888e`** (9) — `zero_row_tail` is present, and better factored than
  upstream's (a shared helper where upstream inlines it twice).
* **`b590c747`** (8) — the `qwen3_5_moe` unfused-expert branch is fork work
  (`f0d50c90`, landed in parallel with upstream), marked `# Fork:`.
* **`a492e47d`** (5) — the remainder is `generate/common.py`'s inline uniform-quant
  path, which the fork replaced deliberately.
* **`e3906673`**'s second half — widening the codec fast path to prefill contradicts
  the fork's measured decode-only split (see `TurboQuantKVCache.__init__`).
* **`e029e2b3`** — library half landed; only its `test_generate.py` half remains
  (folded into 2c).
* **item 9** on the normal text path — upstream bug, see 2b.

---

## 5. Decisions

**Both prior decisions are resolved.** `cfcc36d9` (2a) was approved on 2026-08-09 —
take upstream whole, including the default log interval; it logs counts and rates,
never content. `29b6c00b` (2c) needs no decision: provenance shows it is stale
upstream code, so restoring it is convergence rather than a behaviour choice.

**The only standing question: 61 commits are unpushed.** Nothing has left this
machine. Say if you want them pushed; per-push permission is required each time.

## 6. Validation protocol

```bash
cd mlx_vlm/ && ../.venv/bin/python -m pytest -q ./tests --ignore=tests/test_smoke.py
# expect 2623 passed / 5 skipped / 0 failed; compare FAILED test IDs, never counts

.venv/bin/python dev/check_upstream_parity.py      # missing files
.venv/bin/python dev/check_upstream_symbols.py     # missing defs; warns on stale entries
.venv/bin/python dev/check_upstream_deletions.py   # ~70s; content upstream deleted that we kept
.venv/bin/python dev/check_fork_markers.py         # ~35s; --summary ranks the rollout
.venv/bin/python dev/check_dead_helpers.py         # ~10s; dropped call sites
.venv/bin/python dev/find_dropped_hunks.py         # slow; ranked lead report
.venv/bin/python dev/find_dropped_hunks.py --min-lines 1   # SECOND PASS, see trap 11
```

**Always do that second pass before calling the report clean.** The default
`--min-lines 3` Python floor hid an 18-line behavioural regression
(`f044f36a`'s `server/generation.py` half, §2b) because its individual hunks are 1–2
lines each. The floor exists to suppress noise, and it succeeds at suppressing signal
too.

Format with the pinned hooks (`pre-commit` is not installed in the venv):
`uvx black@26.3.1`, `uvx isort@5.13.2 --profile=black`,
`uvx autoflake@2.2.1 --check --remove-all-unused-imports --ignore-init-module-imports`.

Prove every fix: stash the source hunk, watch the test fail, restore. Fork guards go
in `mlx_vlm/tests/test_dropped_upstream_guards.py` (collected, fork-only). Full suite
before every commit; report fixed/regressed test **IDs**.

---

## 7. Traps — every one of these cost real time

1. **`cd` persists between tool calls, and a wrong relative path returns *empty
   output*, not an error.** Bit **three times** last session: `git log` / `ls-tree` /
   `git diff` silently resolving against `mlx_vlm/mlx_vlm/`, which twice made a file
   look fork-only and once looked like "byte-identical to upstream". **Use
   `git -C /Users/ia87221/ws/mlx-vlm` unconditionally.**
2. **`mlx_vlm.generate` is the re-exported *function*, not the module.** So
   `import mlx_vlm.generate.ar` fails with a confusing `ImportError`, and
   `from mlx_vlm.server import app` hands back the FastAPI instance rather than the
   module. Use `importlib.import_module`.
3. **`cp` is aliased to `cp -i` here.** A restore-from-backup silently did nothing
   and left a neutering edit in the tree. Use `command cp -f`, then check
   `git status`.
4. **Never read a count off a truncated pipe.** `find_dropped_hunks.py | tail -60`
   was counted as "18 commits"; the real number was 34, and the wrong figure reached
   a commit message. Every audit prints its own totals — use those.
5. **"Unreferenced" is not "dead".** autoflake's removal of
   `DEFAULT_DIFFUSION_CONFIDENCE_THRESHOLD` was correct on its own terms and wrong in
   effect: the name was unused *because* `f044f36a` had been dropped, so tidying it
   erased the last evidence of a missing hunk. Before accepting an autoflake removal
   in a diverged file, check whether **upstream** references the name.
6. **A "fully converged" claim about a file says nothing about the feature.**
   `0f359e5` recorded "`apc.py` fully converged with upstream" while all three of its
   new helpers had no call sites.
7. **"Taking upstream's version broke a test" proves nothing about which side is
   right.** It proves the two sides are internally consistent and you are holding
   half of each. Test *all* of a commit's files together — this is how
   `gemma4_assistant/masks.py` got mis-documented as a deliberate divergence.
8. **A test can be edited to tolerate its own bug** — and a legitimate numerical
   relaxation looks identical in the diff. AST-diff *bodies*, and check the change
   log for a recorded reason.
9. **A rule calibrated on nothing fails its own exemplar.** The fork-marker check was
   first written per-hunk, and `models/cache.py` — the one file already following the
   convention — failed it. If a new check fails the thing it was modelled on, the
   check is wrong.
10. **Validate a new detector fires before trusting a clean run**, in both
    directions: break the thing it should catch and see it fail, then restore and see
    it pass.
11. **A detector's own noise floor is a blind spot.** `find_dropped_hunks.py` defaults
    to `--min-lines 3` for Python, and an 18-line dropped hunk made of 1–2 line
    changes fell straight through it — a real regression, missed while "restoring" the
    very commit that owned it. Re-run at `--min-lines 1` before believing a clean
    report, and treat any commit you restore *partially* as unfinished until you have
    diffed its whole `git show --stat` file set against the tree.
12. **"Not an ancestor of `upstream/main`" is not "fork-authored."** Upstream
    squash-merges, so its unsquashed branch commits fail that test while being pure
    upstream work. Check `git log -1 --format=%an` before concluding a commit is ours;
    this is what nearly got `29b6c00b` deferred as a risky "fork rewrite".
